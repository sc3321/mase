"""Shared constants for the BERT quantisation & pruning experiments."""

CHECKPOINT = "prajjwal1/bert-tiny"
TOKENIZER_CHECKPOINT = "bert-base-uncased"
DATASET_NAME = "imdb"
HF_INPUT_NAMES = ["input_ids", "attention_mask", "labels"]
